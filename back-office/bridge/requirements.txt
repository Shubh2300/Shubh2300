# Local EMR Bridge — Python 3.11+ runtime dependencies.
# Install Playwright's Chromium separately: `python -m playwright install chromium`.

fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
playwright>=1.44,<2.0
python-dotenv>=1.0,<2.0

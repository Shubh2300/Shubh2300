"""n8n-office integration adapters.

Reusable Python clients for external services that the n8n office system
talks to (RingCentral SMS/fax, etc.). Each adapter is a thin, dependency-free
module that wraps an upstream API with helpers the n8n workflows can call.
"""
